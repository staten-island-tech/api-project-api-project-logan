const DOMSelectors = {
  grid: document.querySelector(".recipe-grid"),
  cardBack: document.querySelector(".recipe-card-back"),
  searchForm: document.getElementById("search-form"), //to listen for submit
  searchArea: document.getElementById("search-area"), //to get text that user typed in
};

export { DOMSelectors };
